To run:

pip install -r requirements.txt

to start notebooks run:
jupyter notebook
MNIST.ipynb and diabetes.ipynb

Running these will generate most of the plots. However The final
plots regarding data size effect on training time and accuracy are
generated by running:

python timer_mnist.py
